# Network dataset

`raw/` is gitignored — download the dataset yourself and place the CSV(s) here.

Source: Kaggle — search **"NSL-KDD dataset"** (or **"CICIDS2017 dataset"** as an alternative). Unzip and place the CSV files in `data/network/raw/`.
